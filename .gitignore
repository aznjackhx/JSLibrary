node_modules/
dist/
.tsbuild/
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

coverage/

.DS_Store
*.log
.env
.env.*
!.env.example

# Licence signing keys must never be committed.
*.private.pem
private-key.*

# Licence signing keys. The private half must never enter this repository.
license-private-key*.json
*.jwk.json
.verapdf/

# Demo output. Regenerate with `node scripts/render-demo.mjs` rather than
# committing a copy that goes stale against the renderer that made it.
examples/*.pdf
